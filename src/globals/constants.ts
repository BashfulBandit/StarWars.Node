/**
 * Global definition for the system default pageNumber.
 */
export const DEFAULT_PAGE_NUMBER = 1;

/**
 * Global definition for the system default pageSize.
 */
export const DEFAULT_PAGE_SIZE = 25;

/**
 * Global definition for the system default pageCount.
 */
export const DEFAULT_PAGE_COUNT = 0;

/**
 * Global definition for the system default totalCount.
 */
export const DEFAULT_TOTAL_COUNT = 0;
