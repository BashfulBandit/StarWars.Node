const DEFAULT_PORT = 8080;

/**
 * The port the application will listen on. Currently this is hardcoded to a
 * specific port, but can be adjusted later to make it configurable.
 */
export const PORT = DEFAULT_PORT;
