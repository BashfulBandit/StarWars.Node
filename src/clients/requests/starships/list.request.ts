import type { PageRequest } from '~/clients/requests/page.request';

/**
 * Describes a pagination request to get a page of Starships.
 *
 * @public
 */
export type ListStarshipsRequest = PageRequest;
